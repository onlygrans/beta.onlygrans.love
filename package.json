{
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
  "scripts": {
    "watch:sass": "sass --watch _sass:_site/css",
    "build:sass": "sass _sass:_site/css",
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "watch": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
}
